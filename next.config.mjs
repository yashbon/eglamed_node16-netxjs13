/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    // distDir: "production",
    // experimental: {
    //     serverActions: true,
    // },
};

export default nextConfig;
